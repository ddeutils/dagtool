name: 02_group
type: dag
docs: |
  ## Group DAG

  This is the demo dag for passing template data to DAG object.
tags:
  - "group:demo"
  - "tool:true"
  - "domain:demo"
tasks:
  - task: start
    op: empty

  - group: first_group
    upstream: start
    tasks:
      - task: inside
        op: empty

      - group: nested_group
        upstream: inside
        tasks:
          - task: deep
            op: empty
